# Database Connection URL for TimescaleDB / PostgreSQL instance
DATABASE_URL=postgresql+psycopg://nse_user:nse_password@localhost:5433/nse_minute

# Optional: Dedicated Paper Trading Database (defaults to DATABASE_URL if unset)
# PAPER_TRADING_DB_URL=sqlite:///./paper_trading.db
